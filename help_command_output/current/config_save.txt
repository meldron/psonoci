Save psonoci config into a toml file which can be loaded with --config-path

Usage: psonoci config save [OPTIONS] <PATH>

Arguments:
  <PATH>  Output path

Options:
  -o, --overwrite  Only if overwrite is set, psonoci will replace a config
  -h, --help       Print help
